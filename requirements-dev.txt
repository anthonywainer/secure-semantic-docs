# Development dependencies — Python 3.14
# Install with:
#   uv pip install -r requirements.txt -r requirements-dev.txt
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.12
